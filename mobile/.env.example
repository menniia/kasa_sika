# For native builds (Android emulator), use http://10.0.2.2:3000 or your LAN IP.
# For the web target (npm run web), the backend runs on the same machine, so localhost works.
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
